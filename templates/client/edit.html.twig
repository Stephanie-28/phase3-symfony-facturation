{% extends 'base.html.twig' %}
{% block title %}Modifier un client{% endblock %}
{% block body %}

<div class="p-8 max-w-2xl mx-auto">
  <h1 class="text-2xl font-bold text-gray-900 text-center mb-6">MODIFIER UN CLIENT</h1>

  <div class="bg-white rounded-2xl shadow-sm p-8">
    <div class="flex items-center justify-between mb-6">
      <h2 class="text-lg font-bold text-gray-900">Modifier le client</h2>
      <a href="{{ path('app_client_index') }}" class="text-gray-400 hover:text-gray-600 text-xl">✕</a>
    </div>

    {{ include('client/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

  </div>
</div>

{% endblock %}
